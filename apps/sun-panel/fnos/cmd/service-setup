#!/bin/bash

LOG_FILE="${TRIM_PKGVAR}/${TRIM_APPNAME}.log"
PID_FILE="${TRIM_PKGVAR}/${TRIM_APPNAME}.pid"

APP_DIR="${TRIM_APPDEST}"
APP_DATA_DIR="${TRIM_PKGVAR}"

# ===== 新增：创建 docker.sock 软链接 =====
#if [ -S "/var/run/docker.sock" ]; then
#    ln -sf /var/run/docker.sock "$APP_DATA_DIR/docker.sock"
#    chmod 666 $APP_DATA_DIR/docker.sock  # 添加这行
#    echo "✅ docker.sock 软链接已创建: $APP_DATA_DIR/docker.sock -> /var/run/docker.sock"
#else
#    echo "⚠️ 警告: /var/run/docker.sock 不存在，Docker 管理功能可能无法使用"
#fi
# ========================================

SERVICE_COMMAND="$APP_DIR/bin/sun-panel-server $APP_DATA_DIR"
SVC_BACKGROUND=y
SVC_WRITE_PID=y

service_preuninst() {
    pkill -x "sun-panel" 2>/dev/null || true
    sleep 1
    pkill -9 -x "sun-panel" 2>/dev/null || true
}

service_poststop() {
    service_preuninst
}
